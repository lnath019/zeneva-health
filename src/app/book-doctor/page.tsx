import { PageStub } from '@/components/marketing/PageStub';

export default function BookDoctorPage() {
  return (
    <PageStub
      title="Book a Doctor"
      description="Search doctors by specialty, name, or location, view their profiles, and book appointments directly. This public booking flow is being built — for now, sign in to book through your dashboard."
    />
  );
}
