import { PageStub } from '@/components/marketing/PageStub';

export default function HealthDirectoryPage() {
  return (
    <PageStub
      title="Health Directory"
      description="Browse verified doctors, hospitals, and ambulances across Nepal with filters for district, specialty, and services. The full public directory is on its way."
    />
  );
}
