// Single source of truth for site branding.
// Change the logo or default colors here and every page picks it up —
// the palette drives the CSS variables in src/app/layout.tsx and the
// admin theme switcher's default/reset state in src/context/ThemeContext.tsx.

export const LOGO_SRC = '/zeniva-logo.png';
export const LOGO_ALT = 'Zeniva Healthcare';

export const BRAND_COLORS = {
  primary: '#F07818',
  secondary: '#1CA3C0',
  tertiary: '#F4F4F5',
  accent: '#C0263D',
  foreground: '#1A202C',
};
